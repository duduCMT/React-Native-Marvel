import colors from "./colors"
import { LOGO, SCREEN, FONT_SIZE, create } from "./size"
import fonts from "./fonts"

const size = {
  LOGO,
  SCREEN, 
  FONT_SIZE,
  create,
}

export {
  colors,
  fonts,
  size
}